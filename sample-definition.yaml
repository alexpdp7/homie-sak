configs:
  controller_host_1:
    devices:
      example_host_1:
        wol:
          switch:
            datatype: boolean
            settable: true
            retained: false
            run_command:
              - wol
              - 4c:f3:2d:5a:ea:1f
  controller_host_2:
    devices:
      console:
        send_message:
            datatype: boolean
            settable: true
            retained: false
            run_command:
              - echo
              - hi
